export async function getAccessToken(): Promise<string> {
  // Replace with Auth0/Clerk SDK integration.
  // In this starter, tokens are intentionally not persisted to localStorage.
  return 'dev-token';
}
