# Branding assets

Place your images in this folder with these exact names:

- `nav-logo.png` → image used inside the top navigation bar.
- `favicon.png` → browser tab icon.

Recommended format: PNG with transparent background.
