# Frontend notes

For local development:

```bash
cp .env.example .env.local
npm install
npm run dev
```

For a deployed backend, set:

```bash
NEXT_PUBLIC_API_BASE_URL=https://web-production-3a8489.up.railway.app/api/v1
```
